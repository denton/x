SELECT 
    field1 as f1,
    field2 f2,
    distinct field3,
    (field4*5)/6.0-7%8\3+5.93,
    count(*),
    avg(field1) OVER(PARTITION By field2 order by filed3 desc rows between UNBOUNDED PRECEDING and UNBOUNDED FOLLOWING)
window 
into

Where
    f1
having
union
gro
5 as d, 
d,
distinct l,
count(*),
sum(l),
max(4),
min(distinct t) as t,
ROW_NUMBER() OVER(PARTITION BY c
ORDER BY OrderAmount DESC) AS "Row Number", 
CONVERT(VARCHAR(20), SUM(o) OVER(PARTITION BY c ORDER BY OrderAmount DESC ROWS BETWEEN CURRENT ROW AND 1 FOLLOWING), 1) AS CumulativeTotal,
from ./data_log.xlsx 
inner join data.csv on m.id = data.m_id AND s = 1
outer left right join s
Where x = and 1 = 1 or data in (3,4,5)
group by
a, b, s
sort by
a asc,
b,
d desc
c
union 
select q.d, 5,6,4, count(3) 
from {select * from file.xlsx#sheer1